# Codex Autopilot Routing

Use `codex-safe` for low-conflict changes:
- docs
- configs
- CI
- README
- `.github`
- `package.json`

Use `codex-serial` for conflict-prone code changes:
- `src/`
- `server/`
- `ext/`
- runtime logic
